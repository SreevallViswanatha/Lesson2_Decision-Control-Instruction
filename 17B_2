#include <stdio.h>
#include <stdlib.h>

int main()
{
   int qty, dis;
    float rate, tot;
    printf("Enter quantity and rate: ");
    scanf("%d%f", &qty, &rate);

    dis = ((qty>1000) ? 10: 0);


    tot = (qty*rate) - (qty * rate * dis/100);
    printf("Total expenses = Rs. %f\n", tot);


}
